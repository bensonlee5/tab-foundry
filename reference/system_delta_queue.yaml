schema: tab-foundry-system-delta-queue-v1
generated_from_sweep_id: input_norm_none_followup
catalog_path: reference/system_delta_catalog.yaml
canonical_sweep_path: reference/system_delta_sweeps/input_norm_none_followup/sweep.yaml
canonical_queue_path: reference/system_delta_sweeps/input_norm_none_followup/queue.yaml
canonical_matrix_path: reference/system_delta_sweeps/input_norm_none_followup/matrix.md
sweep_id: input_norm_none_followup
parent_sweep_id: input_norm_followup
sweep_status: active
complexity_level: binary_md
anchor_run_id: sd_input_norm_followup_07_dpnb_input_norm_anchor_replay_batch64_sqrt_v2
benchmark_bundle_path: src/tab_foundry/bench/nanotabpfn_openml_binary_medium_v1.json
control_baseline_id: cls_benchmark_linear_v2
comparison_policy: anchor_only
upstream_reference:
  name: nanoTabPFN
  model_source: https://github.com/automl/nanoTabPFN/blob/main/model.py
anchor_surface:
  notes:
  - The locked anchor is benchmark registry run `sd_input_norm_followup_07_dpnb_input_norm_anchor_replay_batch64_sqrt_v2`
    on bundle `nanotabpfn_openml_binary_medium` (10 tasks).
  - The anchor model surface is taken from the registry-resolved staged selection
    labeled `dpnb_input_norm_anchor_replay_batch64_sqrt`.
  - Data and preprocessing remain part of the comparison surface and must stay fixed
    unless the queue row declares that exact dimension.
  - This follow-up anchors on the batch64 sqrt-scaled clipped winner because the earlier
    input-normalization rows were tied and batch size was the only clear source of
    lift.
  - The only live question in this sweep is whether explicit clipping is still needed
    once the better batch64 surface is fixed.
  dimension_table:
  - dimension: feature encoder
    upstream: Scalar feature linear encoder with internal train/test z-score+clip
      handling.
    anchor: Same nano feature encoder path with internal benchmark normalization.
    interpretation: Feature encoding remains close to upstream parity; later deltas
      should be attributed elsewhere.
  - dimension: target conditioning
    upstream: Mean-padded linear target encoder on the direct binary path.
    anchor: Same mean-padded linear target conditioner.
    interpretation: The anchor preserves the upstream label-conditioning mechanism.
  - dimension: cell transformer block
    upstream: Post-norm nanoTabPFN block with feature attention then row attention.
    anchor: Pre-norm cell transformer block without test-self attention.
    interpretation: Block-style changes alter attention flow and should not be conflated
      with tokenizer or readout deltas.
  - dimension: tokenizer
    upstream: One scalar token per feature.
    anchor: Same scalar-per-feature tokenizer.
    interpretation: Tokenization remains aligned with upstream parity.
  - dimension: column encoder
    upstream: None on the upstream direct path.
    anchor: No column-set encoder on the anchor path.
    interpretation: Column-set modeling remains absent and should not explain anchor
      behavior.
  - dimension: row readout
    upstream: Target-column readout from the final cell tensor.
    anchor: Row-CLS pooling path.
    interpretation: Row-pool changes alter how the table summary is extracted and
      should be isolated from context changes.
  - dimension: context encoder
    upstream: None on the upstream direct path.
    anchor: None on the anchor path.
    interpretation: Context encoding remains absent; later context rows will change
      both depth and label-flow semantics.
  - dimension: prediction head
    upstream: Direct binary logits head.
    anchor: Direct binary logits head.
    interpretation: The prediction head remains on the narrow upstream-style binary
      path.
  - dimension: training data surface
    upstream: OpenML notebook tasks only for benchmarking; no repo-local prior-training
      manifest contract.
    anchor: Benchmark bundle `nanotabpfn_openml_binary_medium` (10 tasks) with data
      surface label `anchor_manifest_default`.
    interpretation: Bundle and training-data changes are first-class sweep rows and
      should not be inherited from parent sweep prose.
  - dimension: preprocessing
    upstream: Notebook preprocessing inside the benchmark helper.
    anchor: Benchmark preprocessing surface label `runtime_default`.
    interpretation: Preprocessing changes can alter the effective task definition
      and must be tracked explicitly.
  - dimension: training recipe
    upstream: No repo-local prior-dump training-surface contract.
    anchor: Training surface label `prior_linear_warmup_decay`.
    interpretation: Optimizer and schedule changes are first-class sweep rows, not
      background recipe assumptions.
anchor_context:
  run_id: sd_input_norm_followup_07_dpnb_input_norm_anchor_replay_batch64_sqrt_v2
  experiment: cls_benchmark_staged_prior
  config_profile: cls_benchmark_staged_prior
  model:
    arch: tabfoundry_staged
    benchmark_profile: dpnb_input_norm_anchor_replay_batch64_sqrt
    stage: nano_exact
    stage_label: dpnb_input_norm_anchor_replay_batch64_sqrt
    module_selection:
      allow_test_self_attention: false
      column_encoder: none
      context_encoder: none
      feature_encoder: nano
      head: binary_direct
      post_encoder_norm: none
      row_pool: row_cls
      table_block_style: prenorm
      target_conditioner: mean_padded_linear
      tokenizer: scalar_per_feature
  surface_labels:
    data: anchor_manifest_default
    model: dpnb_input_norm_anchor_replay_batch64_sqrt
    preprocessing: runtime_default
    training: prior_linear_warmup_decay
rows:
- order: 1
  delta_id: dpnb_input_norm_none_batch64_sqrt
  status: completed
  dimension_family: preprocessing
  family: input_normalization
  binary_applicable: true
  description: Remove explicit input normalization on the batch64 sqrt-scaled bridge
    winner and keep the larger-batch surface fixed.
  rationale: Test the user-preferred simpler preprocessing choice on the current best
    tested systems surface rather than reopening the weaker batch32 rows.
  hypothesis: If batch64 with sqrt LR scaling was the real driver of the gain, explicit
    input normalization may be removable without losing the benchmark lift.
  upstream_delta: Upstream nanoTabPFN keeps internal z-score-plus-clip handling on
    the exact path; this repo-local surface can disable explicit pre-encoder normalization
    entirely.
  anchor_delta: Changes only `model.input_normalization` from `train_zscore_clip`
    to `none` while keeping the row-7 batch64 sqrt-scaled bridge surface fixed.
  entangled_legacy_stage: none
  expected_effect: Tests whether batch size was the real driver of the gain and whether
    the batch64 winner can stay competitive without any explicit normalization.
  adequacy_knobs:
  - model.input_normalization
  parameter_adequacy_policy:
    default_plan:
    - Keep the batch64 sqrt-scaled training surface identical to the clipped batch64
      anchor.
    - Compare final ROC AUC and drift directly against the batch64 clipped winner
      before simplifying the default preprocessing surface.
    default_negative_decision: defer
    reject_requires:
    - isolated_change
    - adequacy_plan_complete
    - clearly_worse_without_offsetting_benefit
  applicability_guards: []
  model:
    stage: nano_exact
    stage_label: dpnb_input_norm_none_batch64_sqrt
    input_normalization: none
    module_overrides:
      table_block_style: prenorm
      allow_test_self_attention: false
      row_pool: row_cls
    tfrow_n_heads: 8
    tfrow_n_layers: 3
    tfrow_cls_tokens: 2
    tfrow_norm: layernorm
  data:
    surface_label: anchor_manifest_default
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_linear_warmup_decay
    prior_dump_non_finite_policy: skip
    prior_dump_batch_size: 64
    prior_dump_lr_scale_rule: sqrt
    prior_dump_batch_reference_size: 32
    overrides:
      apply_schedule: true
      optimizer:
        min_lr: 0.000565685424949238
      runtime:
        grad_clip: 1.0
        max_steps: 2500
        trace_activations: true
      schedule:
        stages:
        - name: prior_dump
          steps: 2500
          lr_max: 0.005656854249492381
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Keep the batch64 sqrt-scaled training surface identical to the clipped batch64
    anchor.
  - Compare final ROC AUC and drift directly against the batch64 clipped winner before
    simplifying the default preprocessing surface.
  run_id: sd_input_norm_none_followup_01_dpnb_input_norm_none_batch64_sqrt_v2
  followup_run_ids: []
  decision: defer
  interpretation_status: completed
  confounders: []
  next_action: Keep `sd_input_norm_followup_07_dpnb_input_norm_anchor_replay_batch64_sqrt_v2`
    as the preferred batch64 surface; do not reopen `input_normalization=none` on
    this bundle without new evidence.
  notes:
  - Canonical CUDA rerun registered as `sd_input_norm_none_followup_01_dpnb_input_norm_none_batch64_sqrt_v1`.
  - Canonical CUDA comparison recorded against the batch64 clipped anchor; interpret
    this row before simplifying the preprocessing default.
  - Removing explicit normalization lost 0.0048 final ROC AUC versus the clipped batch64
    anchor, so the simpler `none` surface is not justified on this bundle.
  - Supersedes historical queue run `sd_input_norm_none_followup_01_dpnb_input_norm_none_batch64_sqrt_v1`;
    that registry entry is retained as history only.
  - Canonical rerun registered as `sd_input_norm_none_followup_01_dpnb_input_norm_none_batch64_sqrt_v2`.
  - Canonical benchmark comparison recorded against the locked sweep anchor; interpret
    this row in the full sweep context.
  benchmark_metrics:
    best_step: 1975
    max_grad_norm: 3.1922013759613037
    clipped_step_fraction: 0.0048
    best_log_loss: 0.40393956228046407
    nanotabpfn_best_log_loss: 0.4510985126422435
    final_log_loss: 0.4052133821597301
    nanotabpfn_final_log_loss: 0.453973433403279
    best_brier_score: 0.13316426389715366
    nanotabpfn_best_brier_score: 0.14642705664899075
    final_brier_score: 0.1332384612260224
    nanotabpfn_final_brier_score: 0.14745454546536443
    best_roc_auc: 0.7597362859021641
    nanotabpfn_best_roc_auc: 0.7482958403555632
    final_roc_auc: 0.7586267171926775
    nanotabpfn_final_roc_auc: 0.7446752311186746
    final_minus_best_log_loss: 0.0012738198792660027
    final_minus_best_brier_score: 7.419732886873587e-05
    final_minus_best_roc_auc: -0.0011095687094866413
    drift: -0.0011095687094866413
    nanotabpfn_best: 0.7482958403555632
    nanotabpfn_final: 0.7446752311186746
    delta_final_log_loss: 0.00802589363008882
    delta_final_brier_score: 0.0024911279435832767
    delta_final_roc_auc: -0.004823171215435518
