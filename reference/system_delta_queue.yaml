schema: tab-foundry-system-delta-queue-v1
generated_from_sweep_id: tf_rd_018_lr_warmup_shape_v1
catalog_path: reference/system_delta_catalog.yaml
canonical_sweep_path: reference/system_delta_sweeps/tf_rd_018_lr_warmup_shape_v1/sweep.yaml
canonical_queue_path: reference/system_delta_sweeps/tf_rd_018_lr_warmup_shape_v1/queue.yaml
canonical_matrix_path: reference/system_delta_sweeps/tf_rd_018_lr_warmup_shape_v1/matrix.md
sweep_id: tf_rd_018_lr_warmup_shape_v1
parent_sweep_id: tf_rd_018_optimizer_family_v1
sweep_status: draft
complexity_level: binary_md
anchor_run_id: sd_tf_rd_020_harder_dagzoo_ladder_v1_06_delta_data_manifest_root_tf_rd_020_shift_noise_drift_v2
benchmark_bundle_path: src/tab_foundry/bench/nanotabpfn_openml_binary_medium_v1.json
control_baseline_id: cls_benchmark_linear_v2
external_benchmarks:
- nanotabpfn
training_experiment: cls_benchmark_staged_corpus
training_config_profile: cls_benchmark_staged_corpus
surface_role: architecture_screen
comparison_policy: anchor_only
upstream_reference:
  name: TabICLv2
  model_source: https://arxiv.org/abs/2602.11139
anchor_surface:
  notes:
  - This sweep opens TF-RD-018 issue `#138` after completed issue `#137` kept `schedulefree_adamw`
    as the primary optimizer family on the inherited TF-RD-020 noise-drift anchor.
  - Keep the settled row-first model, preprocessing surface, harder carry-forward
    surface, and harmonized `task_batch_size=1`, `grad_accum_steps=4`, `400`-step
    runtime fixed; only LR and warmup shape are in scope here.
  - The locked anchor is benchmark registry run `sd_tf_rd_020_harder_dagzoo_ladder_v1_06_delta_data_manifest_root_tf_rd_020_shift_noise_drift_v2`
    on `tf_rd_020_shift_noise_drift_v1`; row `01` replays that carried baseline on
    a corrected `400`-step schedule horizon as an explicit local reference for the
    remaining rows.
  - This sweep corrects the inherited parent mismatch where `schedule.stages[0].steps`
    stayed at `2500` despite the short runtime ending at `400` steps, so LR floor
    and warmup reads here should be interpreted on the actual benchmark horizon.
  - The `tf_rd_020_noise_mixture_v1` surface remains inactive fallback context; this
    sweep does not reopen the harder-surface choice unless a later adequacy read becomes
    genuinely confounded.
  - Issue `#139` should not open clipping or budget follow-up until this sweep keeps
    exactly one LR or warmup recipe.
  dimension_table:
  - dimension: locked anchor
    upstream: TabICLv2 motivates synthetic pretraining but does not define this repo-local
      row-first promoted-anchor contract.
    anchor: The kept `row_cls + qass + no tfcol` TF-RD-020 noise-drift winner trained
      on the uncapped `task_batch_size=1`, `grad_accum_steps=4`, `400`-step runtime.
    interpretation: TF-RD-018 issue `#138` must keep the promoted row-first model
      surface fixed while reading LR and warmup evidence.
  - dimension: harder carry-forward surface
    upstream: No upstream reference defines this exact dagzoo harder-surface handoff.
    anchor: The locked harder-surface anchor is `tf_rd_020_shift_noise_drift_v1`,
      inherited directly from TF-RD-020 row `06`.
    interpretation: The TF-RD-020 noise-drift winner remains the carried training-data
      surface throughout this sweep.
  - dimension: optimizer family
    upstream: Not applicable.
    anchor: Completed issue `#137` kept `schedulefree_adamw` on the inherited TF-RD-020
      runtime.
    interpretation: LR and warmup rows should not reopen optimizer-family choice.
  - dimension: LR and warmup shape
    upstream: Adam-style scaling papers motivate treating LR ceiling, LR floor, and
      warmup as coupled knobs rather than independent universal rules.
    anchor: The carried baseline uses `lr_max=0.004`, `optimizer.min_lr=0.0004`, and
      `warmup_ratio=0.05` on a corrected `400`-step schedule horizon layered onto
      the inherited runtime.
    interpretation: Rank rows by final log loss first, final Brier score second, final
      ROC AUC third, and best-to-final drift fourth while treating runtime and clipped-step
      fraction as guardrails only; use row `02` as the `warm0` challenger and row
      `05` as the `warm20` challenger around the corrected short-run baseline.
  - dimension: fallback scope
    upstream: Not applicable.
    anchor: '`tf_rd_020_noise_mixture_v1` is not active in this sweep.'
    interpretation: If this sweep stays ambiguous, defer cleanly or retain exactly
      one fallback schedule variant; do not broaden back to the alternate harder surface
      here.
anchor_context:
  run_id: sd_tf_rd_020_harder_dagzoo_ladder_v1_06_delta_data_manifest_root_tf_rd_020_shift_noise_drift_v2
  experiment: cls_benchmark_staged_corpus
  config_profile: cls_benchmark_staged_corpus
  model:
    arch: tabfoundry_staged
    benchmark_profile: delta_qass_no_column_v3
    stage: qass_context
    stage_label: delta_qass_no_column_v3
    module_selection:
      allow_test_self_attention: true
      column_encoder: none
      context_encoder: qass
      feature_encoder: shared
      head: small_class
      post_encoder_norm: none
      post_stack_norm: none
      row_pool: row_cls
      table_block_residual_scale: none
      table_block_style: prenorm
      target_conditioner: label_token
      tokenizer: shifted_grouped
  surface_labels:
    data: tf_rd_020_shift_noise_drift
    model: delta_qass_no_column_v3
    preprocessing: runtime_default
    training: linear_warmup_decay
rows:
- order: 1
  delta_id: delta_training_tf_rd_018_linear_warmup_decay
  status: ready
  dimension_family: training
  family: schedule
  binary_applicable: true
  description: Keep the anchor model, data, and preprocessing surfaces but use single-stage
    linear decay with a short warmup.
  rationale: Replay the carried schedulefree warmup-decay baseline on a corrected
    short-run `400`-step schedule horizon so issue `#138` has one explicit local reference
    row before reading the schedule variants.
  hypothesis: The corrected `0.05` warmup replay should remain competitive and provide
    the local comparison point for warmup-zero, lower-ceiling, lower-floor, and materially
    longer-warmup probes.
  upstream_delta: Not applicable; this is a repo-local exact-prior training recipe
    change.
  anchor_delta: Keep the inherited `tf_rd_020_shift_noise_drift_v1` data surface,
    preprocessing surface, `schedulefree_adamw` optimizer family, and harmonized `task_batch_size=1`
    with `grad_accum_steps=4` `400`-step runtime fixed, then replay the carried linear-warmup-decay
    recipe on a matching `400`-step schedule horizon.
  entangled_legacy_stage: none
  expected_effect: Reduced early instability versus constant LR or plain linear decay,
    with uncertain final quality impact.
  adequacy_knobs:
  - schedule.stages[0].lr_max
  - optimizer.min_lr
  - schedule.stages[0].warmup_ratio
  parameter_adequacy_policy:
    default_plan:
    - Compare against both the constant-LR anchor and the no-warmup linear-decay row.
    - Treat best/final ROC, post-warmup train-loss variance, and gradient norms as
      the primary interpretation surface.
    default_negative_decision: defer
    reject_requires:
    - isolated_change
    - adequacy_plan_complete
    - clearly_worse_without_offsetting_benefit
  applicability_guards: []
  model:
    stage: qass_context
    stage_label: delta_qass_no_column_v3
    module_overrides:
      column_encoder: none
  dynamic_model_overrides: null
  data:
    surface_label: tf_rd_020_shift_noise_drift
    source: manifest
    corpus_ref: tf_rd_020_shift_noise_drift_v1
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: linear_warmup_decay
    task_batch_size: 1
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_accum_steps: 4
        max_steps: 400
        target_train_seconds: null
        eval_every: 25
        checkpoint_every: 25
        trace_activations: false
        val_batches: 0
      schedule:
        stages:
        - name: stage1
          steps: 400
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Use this row as the explicit local reference for rows `02` through `05`, while
    still comparing every row against the locked sweep anchor.
  - Rank rows by final log loss first, final Brier score second, and final ROC AUC
    third.
  - Treat drift, clipped-step fraction, and runtime as guardrails only.
  execution_policy: benchmark_full
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
  next_action: Run first as the corrected short-run LR/warmup baseline replay, then
    compare the four bounded variants against it and the locked anchor.
  notes:
  - Keep `tf_rd_020_noise_mixture_v1` inactive while reading this row.
  - Align `schedule.stages[0].steps` to `runtime.max_steps=400` so warmup and floor
    reads are interpreted on the actual short-run horizon rather than on the inherited
    `2500`-step parent setting.
  screen_metrics: null
  benchmark_metrics: null
- order: 2
  delta_id: delta_training_tf_rd_018_linear_warmup_decay_warm0
  status: ready
  dimension_family: training
  family: schedule
  binary_applicable: true
  description: Keep the carried short-run warmup-decay surface but remove warmup entirely.
  rationale: Remove warmup while keeping the corrected short-run LR pair fixed so
    issue `#138` can test whether the inherited `0.05` warmup is still necessary on
    the locked noise-drift runtime.
  hypothesis: If the corrected `400`-step runtime is already stable enough, no warmup
    may preserve or improve final quality without materially reopening early instability.
  upstream_delta: Not applicable; this is a repo-local warmup-zero follow-up on the
    corrected short-run warmup-decay surface.
  anchor_delta: Keep the inherited harder-surface runtime fixed and change only `warmup_ratio`
    from `0.05` to `0.0` relative to row `01`.
  entangled_legacy_stage: none
  expected_effect: No warmup may preserve quality if the corrected `400`-step runtime
    is already stable enough without early protection.
  adequacy_knobs:
  - schedule.stages[0].warmup_ratio
  parameter_adequacy_policy:
    default_plan:
    - Compare locally against the corrected short-run warmup-decay baseline while
      keeping LR ceiling, LR floor, optimizer family, and runtime fixed.
    - Prefer it only if benchmark quality stays competitive without reopening obvious
      early instability.
    default_negative_decision: defer
    reject_requires:
    - isolated_change
    - adequacy_plan_complete
    - clearly_worse_without_offsetting_benefit
  applicability_guards: []
  model:
    stage: qass_context
    stage_label: delta_qass_no_column_v3
    module_overrides:
      column_encoder: none
  dynamic_model_overrides: null
  data:
    surface_label: tf_rd_020_shift_noise_drift
    source: manifest
    corpus_ref: tf_rd_020_shift_noise_drift_v1
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: linear_warmup_decay
    task_batch_size: 1
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_accum_steps: 4
        max_steps: 400
        target_train_seconds: null
        eval_every: 25
        checkpoint_every: 25
        trace_activations: false
        val_batches: 0
      schedule:
        stages:
        - name: stage1
          steps: 400
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.0
  parameter_adequacy_plan:
  - Compare directly against row `01` and the locked anchor on final log loss, then
    supporting Brier and ROC AUC.
  - Use early clipped-step behavior only to decide whether the no-warmup recipe reopened
    obvious instability.
  - Do not promote this row on cleaner runtime alone.
  execution_policy: benchmark_full
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
  next_action: Run second as the no-warmup discriminator against the carried baseline.
  notes: []
  screen_metrics: null
  benchmark_metrics: null
- order: 3
  delta_id: delta_training_tf_rd_018_linear_warmup_decay_lr3e3
  status: ready
  dimension_family: training
  family: schedule
  binary_applicable: true
  description: Keep the anchor model, data, and preprocessing surfaces but lower the
    peak LR while keeping the carried warmup-decay floor fixed.
  rationale: Lower the LR ceiling while keeping the carried floor and corrected warmup
    horizon fixed so issue `#138` can test whether the inherited `0.004` peak is still
    too aggressive on the locked noise-drift runtime.
  hypothesis: A slightly lower peak LR may reduce residual clipping and improve final
    retention without needing a different optimizer family or harder-surface branch.
  upstream_delta: Not applicable; this is a repo-local LR-shape follow-up on the settled
    warmup-decay surface.
  anchor_delta: Keep the inherited harder-surface runtime fixed and change only `lr_max`
    from `0.004` to `0.003` relative to row `01`.
  entangled_legacy_stage: none
  expected_effect: Lower peak LR may reduce clipping and improve final retention if
    the carried `0.004` ceiling is still too aggressive on the inherited harder surface.
  adequacy_knobs:
  - schedule.stages[0].lr_max
  parameter_adequacy_policy:
    default_plan:
    - Compare locally against the carried warmup-decay baseline while keeping optimizer
      family, data surface, and runtime fixed.
    - Prefer it only if final quality improves without relying on guardrail metrics
      alone.
    default_negative_decision: defer
    reject_requires:
    - isolated_change
    - adequacy_plan_complete
    - clearly_worse_without_offsetting_benefit
  applicability_guards: []
  model:
    stage: qass_context
    stage_label: delta_qass_no_column_v3
    module_overrides:
      column_encoder: none
  dynamic_model_overrides: null
  data:
    surface_label: tf_rd_020_shift_noise_drift
    source: manifest
    corpus_ref: tf_rd_020_shift_noise_drift_v1
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: linear_warmup_decay
    task_batch_size: 1
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_accum_steps: 4
        max_steps: 400
        target_train_seconds: null
        eval_every: 25
        checkpoint_every: 25
        trace_activations: false
        val_batches: 0
      schedule:
        stages:
        - name: stage1
          steps: 400
          lr_max: 0.003
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Compare directly against row `01` and the locked anchor on final log loss, then
    supporting Brier and ROC AUC.
  - Prefer this row only if the quality win is not merely a guardrail-only stability
    trade.
  - Use the result to bound the LR search downward before issue `#139` opens.
  execution_policy: benchmark_full
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
  next_action: Run after rows `01` and `02` as the lower-ceiling LR probe.
  notes: []
  screen_metrics: null
  benchmark_metrics: null
- order: 4
  delta_id: delta_training_tf_rd_018_linear_warmup_decay_minlr1e4
  status: ready
  dimension_family: training
  family: schedule
  binary_applicable: true
  description: Keep the anchor model, data, and preprocessing surfaces but lower the
    LR floor while keeping the carried peak LR and warmup fixed.
  rationale: Lower the LR floor while keeping the carried peak LR and corrected warmup
    horizon fixed so issue `#138` can test whether the inherited floor is decaying
    too conservatively on the locked noise-drift runtime.
  hypothesis: On a corrected `400`-step schedule horizon, a lower LR floor may preserve
    late plasticity and improve final retention without changing the early-step behavior
    materially.
  upstream_delta: Not applicable; this is a repo-local LR-floor follow-up on the settled
    warmup-decay surface.
  anchor_delta: Keep the inherited harder-surface runtime fixed and change only `optimizer.min_lr`
    from `0.0004` to `0.0001` relative to row `01`.
  entangled_legacy_stage: none
  expected_effect: A lower floor may preserve later plasticity if the carried `0.0004`
    floor is decaying too conservatively on the inherited harder surface.
  adequacy_knobs:
  - optimizer.min_lr
  parameter_adequacy_policy:
    default_plan:
    - Compare locally against the carried warmup-decay baseline while keeping the
      peak LR, warmup, and optimizer family fixed.
    - Prefer it only if final quality improves without a material stability regression.
    default_negative_decision: defer
    reject_requires:
    - isolated_change
    - adequacy_plan_complete
    - clearly_worse_without_offsetting_benefit
  applicability_guards: []
  model:
    stage: qass_context
    stage_label: delta_qass_no_column_v3
    module_overrides:
      column_encoder: none
  dynamic_model_overrides: null
  data:
    surface_label: tf_rd_020_shift_noise_drift
    source: manifest
    corpus_ref: tf_rd_020_shift_noise_drift_v1
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: linear_warmup_decay
    task_batch_size: 1
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0001
        muon_per_parameter_lr: false
      runtime:
        grad_accum_steps: 4
        max_steps: 400
        target_train_seconds: null
        eval_every: 25
        checkpoint_every: 25
        trace_activations: false
        val_batches: 0
      schedule:
        stages:
        - name: stage1
          steps: 400
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Compare directly against row `01` and the locked anchor on final log loss, then
    supporting Brier and ROC AUC.
  - Prefer this row only if it yields a real late-curve gain rather than a small telemetry-only
    change.
  - Treat worse drift or clipping without a quality win as negative evidence for reopening
    the LR floor.
  execution_policy: benchmark_full
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
  next_action: Run after the carried baseline as the lower-floor LR probe.
  notes: []
  screen_metrics: null
  benchmark_metrics: null
- order: 5
  delta_id: delta_training_tf_rd_018_linear_warmup_decay_warm20
  status: ready
  dimension_family: training
  family: schedule
  binary_applicable: true
  description: Keep the carried short-run warmup-decay surface but lengthen warmup
    materially.
  rationale: Materially lengthen warmup while keeping the carried LR pair fixed so
    issue `#138` can test whether the inherited runtime wants more early protection
    before issue `#139` opens clip or budget work.
  hypothesis: A `0.20` warmup may reduce early clipping further on the corrected short-run
    horizon, though it risks underusing the fixed-step budget and hurting final quality.
  upstream_delta: Not applicable; this is a repo-local warmup-length follow-up on
    the corrected short-run warmup-decay surface.
  anchor_delta: Keep the inherited harder-surface runtime fixed and change only `warmup_ratio`
    from `0.05` to `0.20` relative to row `01`.
  entangled_legacy_stage: none
  expected_effect: A `0.20` warmup may reduce early clipping further on the corrected
    `400`-step runtime, though it risks underusing the fixed-step budget.
  adequacy_knobs:
  - schedule.stages[0].warmup_ratio
  parameter_adequacy_policy:
    default_plan:
    - Compare locally against the corrected short-run warmup-decay baseline while
      keeping peak LR, floor, and optimizer family fixed.
    - Prefer it only if benchmark quality and early stability improve together.
    default_negative_decision: defer
    reject_requires:
    - isolated_change
    - adequacy_plan_complete
    - clearly_worse_without_offsetting_benefit
  applicability_guards: []
  model:
    stage: qass_context
    stage_label: delta_qass_no_column_v3
    module_overrides:
      column_encoder: none
  dynamic_model_overrides: null
  data:
    surface_label: tf_rd_020_shift_noise_drift
    source: manifest
    corpus_ref: tf_rd_020_shift_noise_drift_v1
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: linear_warmup_decay
    task_batch_size: 1
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_accum_steps: 4
        max_steps: 400
        target_train_seconds: null
        eval_every: 25
        checkpoint_every: 25
        trace_activations: false
        val_batches: 0
      schedule:
        stages:
        - name: stage1
          steps: 400
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.2
  parameter_adequacy_plan:
  - Compare directly against row `01` and the locked anchor on final log loss, then
    supporting Brier and ROC AUC.
  - Prefer this row only if better early stability comes with a real benchmark quality
    win rather than a budget-underuse regression.
  - Treat this row as the upper warmup bracket; do not promote it solely on cleaner
    clipped-step behavior.
  execution_policy: benchmark_full
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
  next_action: Run after the carried baseline as the materially longer-warmup probe.
  notes: []
  screen_metrics: null
  benchmark_metrics: null
