# @package _global_

defaults:
  - _shared/compact_binary_prior
  - _self_

model:
  arch: tabfoundry_simple
runtime:
  output_dir: outputs/cls_benchmark_linear_simple_prior
logging:
  run_name: cls-benchmark-linear-simple-prior
