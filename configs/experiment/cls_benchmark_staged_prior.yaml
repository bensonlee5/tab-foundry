# @package _global_

defaults:
  - _shared/compact_binary_prior
  - _self_

model:
  arch: tabfoundry_staged
  stage: nano_exact
runtime:
  output_dir: outputs/cls_benchmark_staged_prior
logging:
  run_name: cls-benchmark-staged-prior
