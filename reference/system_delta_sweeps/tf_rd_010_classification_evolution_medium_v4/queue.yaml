schema: tab-foundry-system-delta-sweep-queue-v1
sweep_id: tf_rd_010_classification_evolution_medium_v4
rows:
- order: 1
  delta_ref: delta_data_manifest_root_tf_rd_010_dagzoo_medium_control
  status: ready
  rationale: Establish the TF-RD-010 classification control front before reading any missingness harder-front effect on the smaller medium benchmark rung.
  hypothesis: The evolved sandwich family should first be judged on the TF-RD-010 control corpus (`n_classes_min=2`) against the medium hub manifest.
  anchor_delta: Use the evolved FiLM plus 3-summary-token sandwich contract and train on `tf_rd_010_dagzoo_medium_control_v2` while validating on the hub-owned medium classification manifest.
  model:
    arch: tabfoundry_sandwich
    d_icl: 60
    input_normalization: train_zscore_clip
    many_class_base: 10
    head_hidden_dim: 96
    sandwich_latents: 24
    sandwich_layers: 2
    sandwich_heads: 4
    sandwich_ff_expansion: 2
    sandwich_summary_tokens_per_axis: 3
    sandwich_self_attention_per_cross: 4
    sandwich_pre_row_attention_layers: 1
    sandwich_pre_column_attention_layers: 1
    feature_type_conditioning: film
  data:
    surface_label: tf_rd_010_dagzoo_medium_control
    source: manifest
    corpus_ref: tf_rd_010_dagzoo_medium_control_v2
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_cosine_warmup
    task_batch_size: 16
    prior_dump_batch_size: 64
    synthetic_epoch_budget:
      epochs: 1
      budget_unit: corpus_manifest_records
      prior_dump_batch_size: 64
      allow_partial_final_batch: true
    overrides:
      schedule:
        stages:
        - name: prior_dump
          lr_max: 0.001
          lr_schedule: linear
          warmup_ratio: 0.1
      runtime:
        grad_accum_steps: 4
      optimizer:
        min_lr: 1.0e-05
  parameter_adequacy_plan:
  - Confirm `tab-realdata-hub#1` has materialized the medium classification manifest from `openml_classification_medium_v1.json` before execution.
  - Freeze the legacy `cls_benchmark_linear_multiclass_medium_v1` control baseline before treating any row outcome as a promotion or defer decision.
  - Rank by `final_bpc_at_matched_regime_budget`, then inspect raw log loss, calibration, runtime, and stability as guardrails.
  next_action: Execute this anchor row first under issue `#205`, then continue rows 2-4 as part of the active medium 16x4 rerun.
  notes:
  - '`dagzoo` owns this synthetic training front; `tab-realdata-hub` owns the validation manifest.'
  - 'This corpus keeps the same balanced 144-cell DAGZoo front shape but expands it to 159984 corpus manifest records/tasks: 144 invocation cells x 1111 datasets, still capped at <=1024 total rows per synthetic dataset.'
  - 'Trusted executions use a single synthetic epoch only: one pass over 159984 corpus manifest records/tasks at `prior_dump_batch_size=64`, which resolves to `2500` optimizer steps with an allowed short final batch.'
  - This row remains the intended TF-RD-010 medium reference for missingness and class-imbalance reporting on the medium validation pool.
  - Historical 400-step TF-RD-010 executions, the completed 3-step reset-contract rerun, and the completed clipped `tf_rd_010_classification_evolution_medium_v2` rerun remain historical context only.
  - Trusted rerun work now flows through issues `#202`, `#205`, and `#204`.
  - '`tf_rd_010_classification_evolution_medium_v4` is the active medium rerun contract: exact-shape-compatible manifest tasks are batched with `task_batch_size=16`, `grad_accum_steps=4`, `64` tasks per optimizer update, `runtime.grad_clip=0.0`, linear schedule with `warmup_ratio=0.10`, `lr_max=1e-3`, and `optimizer.min_lr=1e-5`.'
  - '`tf_rd_010_classification_evolution_medium_v3` is preserved historical no-clipping evidence only: rows 1-3 reached very early best benchmark steps and then drifted badly, and row 4 was intentionally stopped rather than extended as canonical evidence.'
  - This row is the anchor row for the active `medium_v4` full rerun.
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
- order: 2
  delta_ref: delta_data_manifest_root_tf_rd_010_missingness_mcar
  status: ready
  rationale: Test whether moderate MCAR exposure improves robustness before structured missingness is considered on the medium validation pool.
  hypothesis: MCAR may improve BPC and log-loss behavior on the evolved sandwich family without adding the stronger structure of MAR or MNAR.
  anchor_delta: Keep the evolved FiLM plus 3-summary-token sandwich contract fixed and replace the control corpus with `tf_rd_010_missingness_mcar_v2`.
  model:
    arch: tabfoundry_sandwich
    d_icl: 60
    input_normalization: train_zscore_clip
    many_class_base: 10
    head_hidden_dim: 96
    sandwich_latents: 24
    sandwich_layers: 2
    sandwich_heads: 4
    sandwich_ff_expansion: 2
    sandwich_summary_tokens_per_axis: 3
    sandwich_self_attention_per_cross: 4
    sandwich_pre_row_attention_layers: 1
    sandwich_pre_column_attention_layers: 1
    feature_type_conditioning: film
  data:
    surface_label: tf_rd_010_missingness_mcar
    source: manifest
    corpus_ref: tf_rd_010_missingness_mcar_v2
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_cosine_warmup
    task_batch_size: 16
    prior_dump_batch_size: 64
    synthetic_epoch_budget:
      epochs: 1
      budget_unit: corpus_manifest_records
      prior_dump_batch_size: 64
      allow_partial_final_batch: true
    overrides:
      schedule:
        stages:
        - name: prior_dump
          lr_max: 0.001
          lr_schedule: linear
          warmup_ratio: 0.1
      runtime:
        grad_accum_steps: 4
      optimizer:
        min_lr: 1.0e-05
  parameter_adequacy_plan:
  - Compare directly against the clean control row before preferring missingness exposure.
  - Keep class-imbalance reporting explicit on the medium rung, but defer any dedicated skew ladder to TF-RD-017.
  - Use the larger hub-backed validation rung later as the main transfer check for any kept missingness front.
  next_action: Execute this row after row 1 anchor promotion as part of the active medium 16x4 rerun under issue `#205`.
  notes:
  - '`dagzoo` owns this synthetic training front; `tab-realdata-hub` owns the validation manifest.'
  - 'This corpus keeps the same balanced 144-cell DAGZoo front shape but expands it to 159984 corpus manifest records/tasks: 144 invocation cells x 1111 datasets, still capped at <=1024 total rows per synthetic dataset.'
  - 'Trusted executions use a single synthetic epoch only: one pass over 159984 corpus manifest records/tasks at `prior_dump_batch_size=64`, which resolves to `2500` optimizer steps with an allowed short final batch.'
  - 'The medium validation pool follows the same hub bundle policy as the large rung: `min_classes=2`, `max_classes=10`, and `max_missing_pct=20.0`.'
  - Historical 400-step TF-RD-010 executions, the completed 3-step reset-contract rerun, and the completed clipped `tf_rd_010_classification_evolution_medium_v2` rerun remain historical context only.
  - Trusted rerun work now flows through issues `#202`, `#205`, and `#204`.
  - '`tf_rd_010_classification_evolution_medium_v4` is the active medium rerun contract: exact-shape-compatible manifest tasks are batched with `task_batch_size=16`, `grad_accum_steps=4`, `64` tasks per optimizer update, `runtime.grad_clip=0.0`, linear schedule with `warmup_ratio=0.10`, `lr_max=1e-3`, and `optimizer.min_lr=1e-5`.'
  - '`tf_rd_010_classification_evolution_medium_v3` is preserved historical no-clipping evidence only: rows 1-3 reached very early best benchmark steps and then drifted badly, and row 4 was intentionally stopped rather than extended as canonical evidence.'
  - This row is approved for the active `medium_v4` full rerun once row 1 establishes the anchor.
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
- order: 3
  delta_ref: delta_data_manifest_root_tf_rd_010_missingness_mar
  status: ready
  rationale: Add a structured missingness row so TF-RD-010 can distinguish random masking from observed-feature-linked masking under the evolved benchmark contract.
  hypothesis: MAR may provide a clearer harder front than MCAR while remaining more interpretable than MNAR.
  anchor_delta: Keep the evolved FiLM plus 3-summary-token sandwich contract fixed and replace the control corpus with `tf_rd_010_missingness_mar_v2`.
  model:
    arch: tabfoundry_sandwich
    d_icl: 60
    input_normalization: train_zscore_clip
    many_class_base: 10
    head_hidden_dim: 96
    sandwich_latents: 24
    sandwich_layers: 2
    sandwich_heads: 4
    sandwich_ff_expansion: 2
    sandwich_summary_tokens_per_axis: 3
    sandwich_self_attention_per_cross: 4
    sandwich_pre_row_attention_layers: 1
    sandwich_pre_column_attention_layers: 1
    feature_type_conditioning: film
  data:
    surface_label: tf_rd_010_missingness_mar
    source: manifest
    corpus_ref: tf_rd_010_missingness_mar_v2
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_cosine_warmup
    task_batch_size: 16
    prior_dump_batch_size: 64
    synthetic_epoch_budget:
      epochs: 1
      budget_unit: corpus_manifest_records
      prior_dump_batch_size: 64
      allow_partial_final_batch: true
    overrides:
      schedule:
        stages:
        - name: prior_dump
          lr_max: 0.001
          lr_schedule: linear
          warmup_ratio: 0.1
      runtime:
        grad_accum_steps: 4
      optimizer:
        min_lr: 1.0e-05
  parameter_adequacy_plan:
  - Compare directly against the clean control plus MCAR and MNAR before preferring structured missingness.
  - Keep class-imbalance reporting explicit on the medium rung, but defer any dedicated skew ladder to TF-RD-017.
  - Use the larger hub-backed validation rung later as the main transfer check for any kept missingness front.
  next_action: Execute this row after row 1 anchor promotion as part of the active medium 16x4 rerun under issue `#205`.
  notes:
  - '`dagzoo` owns this synthetic training front; `tab-realdata-hub` owns the validation manifest.'
  - 'This corpus keeps the same balanced 144-cell DAGZoo front shape but expands it to 159984 corpus manifest records/tasks: 144 invocation cells x 1111 datasets, still capped at <=1024 total rows per synthetic dataset.'
  - 'Trusted executions use a single synthetic epoch only: one pass over 159984 corpus manifest records/tasks at `prior_dump_batch_size=64`, which resolves to `2500` optimizer steps with an allowed short final batch.'
  - 'The medium validation pool follows the same hub bundle policy as the large rung: `min_classes=2`, `max_classes=10`, and `max_missing_pct=20.0`.'
  - Historical 400-step TF-RD-010 executions, the completed 3-step reset-contract rerun, and the completed clipped `tf_rd_010_classification_evolution_medium_v2` rerun remain historical context only.
  - Trusted rerun work now flows through issues `#202`, `#205`, and `#204`.
  - '`tf_rd_010_classification_evolution_medium_v4` is the active medium rerun contract: exact-shape-compatible manifest tasks are batched with `task_batch_size=16`, `grad_accum_steps=4`, `64` tasks per optimizer update, `runtime.grad_clip=0.0`, linear schedule with `warmup_ratio=0.10`, `lr_max=1e-3`, and `optimizer.min_lr=1e-5`.'
  - '`tf_rd_010_classification_evolution_medium_v3` is preserved historical no-clipping evidence only: rows 1-3 reached very early best benchmark steps and then drifted badly, and row 4 was intentionally stopped rather than extended as canonical evidence.'
  - This row is approved for the active `medium_v4` full rerun once row 1 establishes the anchor.
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
- order: 4
  delta_ref: delta_data_manifest_root_tf_rd_010_missingness_mnar
  status: ready
  rationale: Keep one strongest missingness row in the first draft package so TF-RD-010 can compare MCAR, MAR, and MNAR under the same medium benchmark contract.
  hypothesis: MNAR may be the hardest missingness front, but it may also be the least interpretable candidate for the first evolved benchmark package.
  anchor_delta: Keep the evolved FiLM plus 3-summary-token sandwich contract fixed and replace the control corpus with `tf_rd_010_missingness_mnar_v2`.
  model:
    arch: tabfoundry_sandwich
    d_icl: 60
    input_normalization: train_zscore_clip
    many_class_base: 10
    head_hidden_dim: 96
    sandwich_latents: 24
    sandwich_layers: 2
    sandwich_heads: 4
    sandwich_ff_expansion: 2
    sandwich_summary_tokens_per_axis: 3
    sandwich_self_attention_per_cross: 4
    sandwich_pre_row_attention_layers: 1
    sandwich_pre_column_attention_layers: 1
    feature_type_conditioning: film
  data:
    surface_label: tf_rd_010_missingness_mnar
    source: manifest
    corpus_ref: tf_rd_010_missingness_mnar_v2
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_cosine_warmup
    task_batch_size: 16
    prior_dump_batch_size: 64
    synthetic_epoch_budget:
      epochs: 1
      budget_unit: corpus_manifest_records
      prior_dump_batch_size: 64
      allow_partial_final_batch: true
    overrides:
      schedule:
        stages:
        - name: prior_dump
          lr_max: 0.001
          lr_schedule: linear
          warmup_ratio: 0.1
      runtime:
        grad_accum_steps: 4
      optimizer:
        min_lr: 1.0e-05
  parameter_adequacy_plan:
  - Compare directly against the clean control plus MCAR and MAR before preferring the strongest self-masking option.
  - Keep class-imbalance reporting explicit on the medium rung, but defer any dedicated skew ladder to TF-RD-017.
  - Use the larger hub-backed validation rung later as the main transfer check for any kept missingness front.
  next_action: Execute this row after row 1 anchor promotion as part of the active medium 16x4 rerun under issue `#205`.
  notes:
  - '`dagzoo` owns this synthetic training front; `tab-realdata-hub` owns the validation manifest.'
  - 'This corpus keeps the same balanced 144-cell DAGZoo front shape but expands it to 159984 corpus manifest records/tasks: 144 invocation cells x 1111 datasets, still capped at <=1024 total rows per synthetic dataset.'
  - 'Trusted executions use a single synthetic epoch only: one pass over 159984 corpus manifest records/tasks at `prior_dump_batch_size=64`, which resolves to `2500` optimizer steps with an allowed short final batch.'
  - 'The medium validation pool follows the same hub bundle policy as the large rung: `min_classes=2`, `max_classes=10`, and `max_missing_pct=20.0`.'
  - Historical 400-step TF-RD-010 executions, the completed 3-step reset-contract rerun, and the completed clipped `tf_rd_010_classification_evolution_medium_v2` rerun remain historical context only.
  - Trusted rerun work now flows through issues `#202`, `#205`, and `#204`.
  - '`tf_rd_010_classification_evolution_medium_v4` is the active medium rerun contract: exact-shape-compatible manifest tasks are batched with `task_batch_size=16`, `grad_accum_steps=4`, `64` tasks per optimizer update, `runtime.grad_clip=0.0`, linear schedule with `warmup_ratio=0.10`, `lr_max=1e-3`, and `optimizer.min_lr=1e-5`.'
  - '`tf_rd_010_classification_evolution_medium_v3` is preserved historical no-clipping evidence only: rows 1-3 reached very early best benchmark steps and then drifted badly, and row 4 was intentionally stopped rather than extended as canonical evidence.'
  - This row is approved for the active `medium_v4` full rerun once row 1 establishes the anchor.
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
