# @package _global_

task: classification
model:
  feature_group_size: 1
runtime:
  output_dir: outputs/cls_manyclass_eval
eval:
  split: test
  max_batches: 128
logging:
  run_name: cls-manyclass-eval
