# @package _global_

defaults:
  - _shared/compact_binary_short
  - _self_

model:
  arch: tabfoundry_staged
  stage: nano_exact
runtime:
  output_dir: outputs/cls_benchmark_staged
  trace_activations: true
logging:
  run_name: cls-benchmark-staged
