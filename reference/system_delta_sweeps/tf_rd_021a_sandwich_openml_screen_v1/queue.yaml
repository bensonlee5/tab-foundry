schema: tab-foundry-system-delta-sweep-queue-v1
sweep_id: tf_rd_021a_sandwich_openml_screen_v1
rows:
- order: 1
  delta_ref: delta_tf_rd_021a_sandwich_replay_v1
  status: completed
  rationale: Reproduce the current repeated-input sandwich baseline on the locked
    batch64-sqrt prior surface so [#179](https://github.com/bensonlee5/tab-foundry/issues/179)
    has one explicit local comparator before the latent-count rows are interpreted.
  hypothesis: If the current sandwich architecture is viable at all on the fast nanoTabPFN
    surface, this row should train cleanly and land within decision-relevant distance
    of the staged prior anchor.
  anchor_delta: Keep the nanoTabPFN medium bundle, `prior_linear_warmup_decay` batch64-sqrt
    recipe, and small-class classification contract fixed, then swap the staged row-cls
    prior anchor for the current repeated-input sandwich baseline.
  model:
    arch: tabfoundry_sandwich
    d_icl: 96
    input_normalization: train_zscore_clip
    many_class_base: 2
    head_hidden_dim: 128
    norm_type: layernorm
    sandwich_latents: 48
    sandwich_layers: 8
    sandwich_heads: 8
    sandwich_ff_expansion: 2
  data:
    source: legacy_prior
    surface_label: legacy_prior
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_linear_warmup_decay
    task_batch_size: 1
    prior_dump_non_finite_policy: skip
    prior_dump_batch_size: 64
    prior_dump_lr_scale_rule: sqrt
    prior_dump_batch_reference_size: 32
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_clip: 1.0
        max_steps: 2500
        trace_activations: false
      schedule:
        stages:
        - name: prior_dump
          steps: 2500
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Run this row first so the sandwich screen has one explicit local baseline on the
    benchmark prior surface.
  - Compare all later sandwich rows against both this replay and the staged prior
    anchor.
  - Keep the repeated-input architecture fixed while reading latent count and width.
  parent_delta_ref: null
  run_id: sd_tf_rd_021a_sandwich_nanotabpfn_screen_v1_01_delta_tf_rd_021a_sandwich_replay_v1_v1
  followup_run_ids: []
  decision: defer
  interpretation_status: completed
  confounders: []
  next_action: Close TF-RD-021A with this row as explicit negative evidence for the
    summary-bottleneck sandwich, then hand successor architecture work to TF-RD-021B
    under [#178](https://github.com/bensonlee5/tab-foundry/issues/178).
  notes:
  - This row is the local sandwich reference for the queue, not a promotion claim.
  - This row trained stably, but it underperformed the locked staged anchor badly
    enough that the latent and width ladder is no longer the preferred next discriminator.
  - Canonical rerun registered as `sd_tf_rd_021a_sandwich_nanotabpfn_screen_v1_01_delta_tf_rd_021a_sandwich_replay_v1_v1`.
  - Canonical benchmark comparison recorded against the locked sweep anchor; interpret
    this row in the full sweep context.
  benchmark_metrics:
    best_step: 1325
    max_grad_norm: 6.598875522613525
    clipped_step_fraction: 0.0148
    primary_external_benchmark: nanotabpfn
    primary_external_label: nanoTabPFN
    best_log_loss: 0.5494122402096481
    primary_external_best_log_loss: 0.45712715197456966
    nanotabpfn_best_log_loss: 0.45712715197456966
    final_log_loss: 0.554868714865437
    primary_external_final_log_loss: 0.4577763981079566
    nanotabpfn_final_log_loss: 0.4577763981079566
    best_brier_score: 0.37349201528868586
    primary_external_best_brier_score: 0.2963550319556705
    nanotabpfn_best_brier_score: 0.2963550319556705
    final_brier_score: 0.3773978076360224
    primary_external_final_brier_score: 0.29670557439218176
    nanotabpfn_final_brier_score: 0.29670557439218176
    best_roc_auc: 0.6055606875128555
    primary_external_best_roc_auc: 0.7529559755496036
    nanotabpfn_best_roc_auc: 0.7529559755496036
    final_roc_auc: 0.6224489632492666
    primary_external_final_roc_auc: 0.751511360184512
    nanotabpfn_final_roc_auc: 0.751511360184512
    final_minus_best_log_loss: 0.005456474655788912
    final_minus_best_brier_score: 0.003905792347336512
    final_minus_best_roc_auc: 0.01688827573641105
    drift: 0.01688827573641105
    primary_external_best: 0.7529559755496036
    primary_external_final: 0.751511360184512
    nanotabpfn_best: 0.7529559755496036
    nanotabpfn_final: 0.751511360184512
    delta_final_log_loss: 0.15768122633579573
    delta_final_brier_score: 0.11590314107114413
    delta_final_roc_auc: -0.14100092515884644
- order: 2
  delta_ref: delta_tf_rd_021a_sandwich_latents24_v1
  status: deferred_separate_workstream
  rationale: Keep the lower-latent bracket as backlog evidence only because row `01`
    showed the summary-bottleneck architecture is the limiting factor, not an obviously
    under-tuned latent budget.
  hypothesis: If the current sandwich replay is carrying more latent memory than the
    prior benchmark needs, halving `sandwich_latents` to `24` may keep most of the
    quality while softening cost.
  anchor_delta: Keep row `01` fixed and change only `sandwich_latents` from `48` to
    `24`.
  model:
    arch: tabfoundry_sandwich
    d_icl: 96
    input_normalization: train_zscore_clip
    many_class_base: 2
    head_hidden_dim: 128
    norm_type: layernorm
    sandwich_latents: 24
    sandwich_layers: 8
    sandwich_heads: 8
    sandwich_ff_expansion: 2
  data:
    source: legacy_prior
    surface_label: legacy_prior
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_linear_warmup_decay
    task_batch_size: 1
    prior_dump_non_finite_policy: skip
    prior_dump_batch_size: 64
    prior_dump_lr_scale_rule: sqrt
    prior_dump_batch_reference_size: 32
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_clip: 1.0
        max_steps: 2500
        trace_activations: false
      schedule:
        stages:
        - name: prior_dump
          steps: 2500
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Run after the sandwich replay as the lower latent-count bracket.
  - Use this row and the high-latent row to decide whether width follow-up should
    stay near the replay or move upward.
  parent_delta_ref: delta_tf_rd_021a_sandwich_replay_v1
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: blocked
  confounders: []
  next_action: Do not run inside TF-RD-021A. Revisit latent-count movement only if
    the TF-RD-021B successor replay becomes viable enough to justify a fresh capacity
    ladder.
  notes:
  - Treat this as abandoned first-pass backlog evidence, not as a live compact-model
    discriminator.
  - The canonical row `01` replay closed the sweep before latent-count follow-up because
    the architecture boundary looked more limiting than latent count.
- order: 3
  delta_ref: delta_tf_rd_021a_sandwich_latents96_v1
  status: deferred_separate_workstream
  rationale: Keep the upper-latent bracket as backlog evidence only because row `01`
    did not justify spending more budget on the same summary-bottleneck topology.
  hypothesis: If the repeated-input sandwich is memory-bottlenecked rather than width-bottlenecked
    on this prior surface, doubling `sandwich_latents` to `96` should improve fit
    enough to justify the blocked width follow-up around the high-latent row.
  anchor_delta: Keep row `01` fixed and change only `sandwich_latents` from `48` to
    `96`.
  model:
    arch: tabfoundry_sandwich
    d_icl: 96
    input_normalization: train_zscore_clip
    many_class_base: 2
    head_hidden_dim: 128
    norm_type: layernorm
    sandwich_latents: 96
    sandwich_layers: 8
    sandwich_heads: 8
    sandwich_ff_expansion: 2
  data:
    source: legacy_prior
    surface_label: legacy_prior
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_linear_warmup_decay
    task_batch_size: 1
    prior_dump_non_finite_policy: skip
    prior_dump_batch_size: 64
    prior_dump_lr_scale_rule: sqrt
    prior_dump_batch_reference_size: 32
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_clip: 1.0
        max_steps: 2500
        trace_activations: false
      schedule:
        stages:
        - name: prior_dump
          steps: 2500
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Run after the replay and the low-latent row to bracket the first latent-count
    read.
  - If this row is clearly best, use it as the upper candidate for the blocked width
    follow-up.
  parent_delta_ref: delta_tf_rd_021a_sandwich_replay_v1
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: blocked
  confounders: []
  next_action: Do not run inside TF-RD-021A. Revisit high-latent capacity only after
    TF-RD-021B establishes a stronger successor replay on the same locked prior surface.
  notes:
  - Treat this as abandoned first-pass backlog evidence, not as an active width-follow-up
    gate.
  - Use runtime and any later local VRAM observation only as guardrails if this row
    is ever revived under successor-architecture work.
- order: 4
  delta_ref: delta_tf_rd_021a_sandwich_width128_latents48_v1
  status: deferred_separate_workstream
  rationale: Keep the baseline-width follow-up as backlog evidence only because the
    completed replay row did not justify widening the same summary-bottleneck architecture.
  hypothesis: If the replay latent count remains live after the first screen but still
    looks representation-limited, widening `d_icl` to `128` may recover quality without
    needing the larger latent bank.
  anchor_delta: Keep row `01` fixed and change only `d_icl` from `96` to `128`.
  model:
    arch: tabfoundry_sandwich
    d_icl: 128
    input_normalization: train_zscore_clip
    many_class_base: 2
    head_hidden_dim: 128
    norm_type: layernorm
    sandwich_latents: 48
    sandwich_layers: 8
    sandwich_heads: 8
    sandwich_ff_expansion: 2
  data:
    source: legacy_prior
    surface_label: legacy_prior
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_linear_warmup_decay
    task_batch_size: 1
    prior_dump_non_finite_policy: skip
    prior_dump_batch_size: 64
    prior_dump_lr_scale_rule: sqrt
    prior_dump_batch_reference_size: 32
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_clip: 1.0
        max_steps: 2500
        trace_activations: false
      schedule:
        stages:
        - name: prior_dump
          steps: 2500
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Unblock only after rows `01` through `03` establish whether the replay latent
    count remains a live candidate.
  - If unblocked, compare directly against the replay row before treating width as
    a needed axis.
  parent_delta_ref: delta_tf_rd_021a_sandwich_replay_v1
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: blocked
  confounders: []
  next_action: Do not execute in TF-RD-021A. Consider width movement only on the
    successor architecture if a fresh replay shows the topology is viable but still
    width-limited.
  notes:
  - This row is no longer blocked on the latent screen; it is deferred because the
    current topology underperformed before width became a meaningful next read.
  - Do not treat width as a rescue attempt for the closed summary-bottleneck replay.
- order: 5
  delta_ref: delta_tf_rd_021a_sandwich_width128_latents96_v1
  status: deferred_separate_workstream
  rationale: Keep the high-latent width follow-up as backlog evidence only because
    the completed replay row did not justify widening the same summary-bottleneck
    architecture.
  hypothesis: If the `96`-latent row is viable but still width-limited, the joint
    `d_icl=128` plus `sandwich_latents=96` setting may become the first serious second-pass
    size probe.
  anchor_delta: Keep row `03` fixed and change only `d_icl` from `96` to `128`.
  model:
    arch: tabfoundry_sandwich
    d_icl: 128
    input_normalization: train_zscore_clip
    many_class_base: 2
    head_hidden_dim: 128
    norm_type: layernorm
    sandwich_latents: 96
    sandwich_layers: 8
    sandwich_heads: 8
    sandwich_ff_expansion: 2
  data:
    source: legacy_prior
    surface_label: legacy_prior
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: prior_linear_warmup_decay
    task_batch_size: 1
    prior_dump_non_finite_policy: skip
    prior_dump_batch_size: 64
    prior_dump_lr_scale_rule: sqrt
    prior_dump_batch_reference_size: 32
    overrides:
      apply_schedule: true
      optimizer:
        name: schedulefree_adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_clip: 1.0
        max_steps: 2500
        trace_activations: false
      schedule:
        stages:
        - name: prior_dump
          steps: 2500
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Unblock only if the high-latent row is viable enough to justify a width follow-up.
  - Treat this as a bounded second-pass size probe rather than a new architecture
    fork.
  parent_delta_ref: delta_tf_rd_021a_sandwich_latents96_v1
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: blocked
  confounders: []
  next_action: Do not execute in TF-RD-021A. Consider this only if the TF-RD-021B
    successor replay wins enough evidence to justify a new capacity screen.
  notes:
  - This row remains historical backlog evidence around the abandoned high-latent
    bracket.
  - Do not read this as an active next step for the closed TF-RD-021A sweep.
