schema: tab-foundry-system-delta-sweep-queue-v1
sweep_id: binary_md_v3
rows:
- order: 1
  delta_ref: delta_preproc_impute_missing_off
  status: deferred_separate_workstream
  rationale: Keep the missingness-only placeholder attached to the promoted shared+LayerNorm
    anchor while missingness work proceeds on a separate branch.
  hypothesis: Missingness-specific conclusions should wait for a dedicated workstream
    with explicit missing-valued training and evaluation inputs.
  anchor_delta: Changes only preprocessing by disabling runtime mean imputation while
    keeping the shared+LayerNorm anchor model, data surface, and training recipe fixed.
  model:
    stage_label: anchor_model
  data:
    surface_label: anchor_manifest_default
  preprocessing:
    surface_label: runtime_no_impute
    overrides:
      impute_missing: false
  training:
    surface_label: prior_constant_lr
    overrides: {}
  parameter_adequacy_plan:
  - If the result is weak, note whether the benchmark tasks actually contain meaningful
    missingness on the support side.
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: blocked
  confounders: []
  next_action: Do not run on the main no-missing campaign; revisit only in a dedicated
    missingness workstream with explicit missing-valued training and evaluation inputs.
  notes:
  - Placeholder row only on this branch; missingness implementation lives on a separate
    branch.
