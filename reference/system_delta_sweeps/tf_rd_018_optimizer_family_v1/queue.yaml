schema: tab-foundry-system-delta-sweep-queue-v1
sweep_id: tf_rd_018_optimizer_family_v1
rows:
- order: 1
  delta_ref: delta_training_adamw
  status: ready
  rationale: Compare plain AdamW against the locked TF-RD-020 noise-drift winner
    so issue `#137` can isolate optimizer family after the uncapped harder-front
    ladder removed dataset row caps.
  hypothesis: Plain AdamW may improve late retention or reduce drift on the harder
    surface if schedulefree dynamics are masking the best benchmark-facing read.
  anchor_delta: Keep the inherited `tf_rd_020_shift_noise_drift_v1` data surface,
    preprocessing surface, benchmark bundle, and harmonized `task_batch_size=1`
    with `grad_accum_steps=4` `400`-step runtime fixed, but replace
    `schedulefree_adamw` with plain `adamw`.
  model:
    stage: qass_context
    stage_label: delta_qass_no_column_v3
    module_overrides:
      column_encoder: none
  data:
    surface_label: tf_rd_020_shift_noise_drift
    source: manifest
    corpus_ref: tf_rd_020_shift_noise_drift_v1
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: linear_warmup_decay
    task_batch_size: 1
    overrides:
      apply_schedule: true
      optimizer:
        name: adamw
        require_requested: true
        weight_decay: 0.0
        betas:
        - 0.9
        - 0.999
        min_lr: 0.0004
        muon_per_parameter_lr: false
      runtime:
        grad_accum_steps: 4
        max_steps: 400
        target_train_seconds: null
        eval_every: 25
        checkpoint_every: 25
        trace_activations: false
        val_batches: 0
      schedule:
        stages:
        - name: stage1
          steps: 2500
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Treat this as a bounded optimizer-family comparison on the locked TF-RD-020
    harder-surface anchor.
  - Compare final log loss first, then final Brier score, final ROC AUC, and
    best-to-final drift against the locked anchor and row `02`.
  - Use NaNs, OOM, clipped-step fraction, and obvious LR-transfer artifacts as guardrails
    before making a reject call.
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
  next_action: Run directly against the locked TF-RD-020 noise-drift anchor, then
    compare against row `02`.
  notes: []
- order: 2
  delta_ref: delta_training_muon
  status: ready
  rationale: Compare Muon against the locked TF-RD-020 noise-drift winner so
    issue `#137` can read whether a more specialized optimizer changes the adequacy
    story on the inherited harder surface.
  hypothesis: Muon may improve convergence or late retention on the harder surface,
    but a weak result should only matter if it is not obviously an LR-transfer problem.
  anchor_delta: Keep the inherited `tf_rd_020_shift_noise_drift_v1` data surface,
    preprocessing surface, benchmark bundle, and harmonized `task_batch_size=1`
    with `grad_accum_steps=4` `400`-step runtime fixed, but replace
    `schedulefree_adamw` with `muon`.
  model:
    stage: qass_context
    stage_label: delta_qass_no_column_v3
    module_overrides:
      column_encoder: none
  data:
    surface_label: tf_rd_020_shift_noise_drift
    source: manifest
    corpus_ref: tf_rd_020_shift_noise_drift_v1
  preprocessing:
    surface_label: runtime_default
  training:
    surface_label: linear_warmup_decay
    task_batch_size: 1
    overrides:
      apply_schedule: true
      optimizer:
        name: muon
        require_requested: true
        weight_decay: 0.01
        betas:
        - 0.9
        - 0.95
        min_lr: 1.0e-06
        muon_per_parameter_lr: true
        muon_lr_scale_base: 0.2
        muon_partition_non2d: true
      runtime:
        grad_accum_steps: 4
        max_steps: 400
        target_train_seconds: null
        eval_every: 25
        checkpoint_every: 25
        trace_activations: false
        val_batches: 0
      schedule:
        stages:
        - name: stage1
          steps: 2500
          lr_max: 0.004
          lr_schedule: linear
          warmup_ratio: 0.05
  parameter_adequacy_plan:
  - Treat this as a bounded optimizer-family comparison only on the locked TF-RD-020
    harder-surface anchor.
  - Compare final log loss first, then final Brier score, final ROC AUC, and
    best-to-final drift against the locked anchor and row `01`.
  - If weak, defer rather than reject unless the result is clearly worse without an
    offsetting stability or runtime benefit and does not look like pure LR-transfer misspecification.
  run_id: null
  followup_run_ids: []
  decision: null
  interpretation_status: pending
  confounders: []
  next_action: Run directly against the locked TF-RD-020 noise-drift anchor; if
    the result is close, retain at most one fallback optimizer family for issue `#138`.
  notes: []
